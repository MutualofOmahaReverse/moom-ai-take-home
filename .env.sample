# Copy this to .env and fill it in. Nothing here is secret.
#
# You need your own Anthropic API key. If you do not have one, the app still
# runs: the classifier falls back to a stub and every other part of the system
# works. See "Running without a key" in the README.
ANTHROPIC_API_KEY=sk-ant-...
