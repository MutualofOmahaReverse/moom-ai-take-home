# You do not need this file. Copy it only if you want to use a real model.
#
# With no key set, classification and extraction run offline -- see
# api/app/services/offline.py. That is the default, and nothing in the tickets
# requires a key.
ANTHROPIC_API_KEY=
